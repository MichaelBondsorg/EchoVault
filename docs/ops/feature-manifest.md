# Engram Feature Manifest

> This file is GENERATED by `node scripts/generate-feature-manifest.js` from src/config/flags.js, scripts/flip-flag.mjs, functions/src/models/registry.js, and a repo-wide scan (functions/src/**, the functions/index.js monolith, and src/**) for getServerFlag()/getModelFlag()/getFlag()/dark-constant call sites. Do not hand-edit — edit the source files (or this generator's curated annotation maps) and regenerate. A vitest check (scripts/__tests__/feature-manifest.test.js) fails CI if this file drifts from a fresh regeneration.
>
> **This table documents code DEFAULTS and rollback MECHANISMS, not live production state.** Live `config/flags` values change without a commit (they are Firestore field writes via `scripts/flip-flag.mjs`) — cross-check PROJECT_STATUS.md's Active Work section for what is actually flipped ON in prod today; treat any such claim there as a point-in-time note, not something this generator re-verifies.

## Flags (client `src/config/flags.js` + server-only + model-boolean)

The "Prod flip mechanism" and "Rollback" cells are always a single, cleanly copy-pasteable command (or a plain sentence when nothing is wired) — timing is reported separately in "Takes effect" so a reader never has to strip prose off a command before pasting it.

| Flag | Default | Prod flip mechanism | Takes effect | Rollback | Surface | KPI to watch after flip | Owner-gate notes | Gaps |
|---|---|---|---|---|---|---|---|---|
| `contextSpaces` | OFF (false) | node scripts/flip-flag.mjs contextSpaces true | client: next app load; server: within 60s (getServerFlag 60s in-process cache) — no deploy | node scripts/flip-flag.mjs contextSpaces false | src/services/spaces/{spacesService,scopeFilter}.js — entries scoped via `spaceId`, strict filter enforced at every retrieval seam except digest/reports (stay cross-space until R2+). [derived: read via getServerFlag() at functions/src/analysis/orchestrator.js:133] | No dedicated log line found — watch scopeFilter's own test suite plus a manual cold-start check. | Checklist item (6): R1 leftover, still open — manual cold-start last-space check required before this flag defaults on. | — |
| `coreFirstSave` | ON (true) | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.coreFirstSave` required; add "coreFirstSave" to ALLOWED to get the CLI typo-guard. | client: next app load — no deploy | Direct Admin SDK/Firestore-console write: delete/false the `config/flags.coreFirstSave` field. | Entry save path — WS-C core-first save (write locally, sync in background). | No dedicated log line. Watch Crashlytics for save-path errors and the capture-stage telemetry `queueDepth`/`errorCode` fields (functions/src/telemetry/stageLog.js) for a spike after flip. | None — already default ON, validated by tests per CLAUDE.md. | No flip-flag.mjs entry (see mechanism column). |
| `gentleRevisit` | OFF (false) | node scripts/flip-flag.mjs gentleRevisit true | client: next app load; server: within 60s (getServerFlag 60s in-process cache) — no deploy | node scripts/flip-flag.mjs gentleRevisit false | functions/src/revisit/selectRevisits.js — server-side, no-LLM heuristic job writing `revisit_queue`, suppressed by `revisit_exclusions`; enforces the plan/brief's six non-negotiable safety rules (GR1 added a legacy-fail-closed eligibility rule + retuned the mood floor within that set), PLUS two separate GR1 per-user "skip today" gates layered on top before selection even runs: a current-state pause gate (recent safety signal/sustained low mood) and a weekly cadence gate. [derived: read via getServerFlag() at functions/src/revisit/selectRevisits.js:653] | `revisit_queue`/`revisit_exclusions` document counts and outcome fields (shown/dismissed) — owner-scoped Firestore query, no dashboard. | Checklist item (4): safety memo docs/quality/gentle-revisit-safety.md SIGNED 2026-07-22 (both boxes). Still gated by checklist item (5) — flip LAST, after insightReceipts/voiceChapters/reflectionRecipes/sessionPrep. The memo gate is separate from and in addition to the flag itself. | — |
| `insightBudget` | OFF (false) | node scripts/flip-flag.mjs insightBudget true | client: next app load — no deploy | node scripts/flip-flag.mjs insightBudget false | src/services/insights/insightBudget.js — hard daily/weekly proactive-insight caps by mode (quiet/balanced/exploratory); never lowers confidence gates to fill the quota. | No dedicated log line found — watch for user-visible insight-starvation reports vs. cap math in insightBudget.js. | R1, shipped. | — |
| `insightClaims` | OFF (false) | node scripts/flip-flag.mjs insightClaims true | client: next app load; server: within 60s (getServerFlag 60s in-process cache) — no deploy | node scripts/flip-flag.mjs insightClaims false | src/services/insights/{observations,testingLedger}.js + src/services/insights/claims/ — canonical, versioned, immutable-with-lineage InsightClaim store; writes to `insight_claims` and `testing_ledger` collections. [derived: read via getServerFlag() at functions/src/insights/conversationReady.js:135, functions/src/insights/conversationReady.js:152] | `insight_claims` document counts by status (verified/expired/suppressed) and `testing_ledger` candidate counts per family — owner-scoped Firestore query, no dashboard. | Checklist item (11): no memo required — flip after Michael eyeballs claim cards on his own data (deterministic wording, no LLM at Phase 1). | — |
| `insightReceipts` | OFF (false) | node scripts/flip-flag.mjs insightReceipts true | client: next app load — no deploy | node scripts/flip-flag.mjs insightReceipts false | src/services/insights/receipts.js + InsightControlCenter/ReceiptSheet — every proactive insight carries {sources, scope, timeWindow, sampleSize, missingness, versions} regardless of the flag; the flag gates UI visibility + correction flow via `source_exclusions`. | `source_exclusions` document counts (owner-scoped query) for correction volume; recompute.js's `onSourcesChanged` fan-out is same-seconds, so a lag there is the failure signal to watch. | Checklist item (5): first in the recommended R2 flip order (insightReceipts -> voiceChapters -> reflectionRecipes -> sessionPrep -> gentleRevisit). | — |
| `intentAbstainAudit` | OFF (false) | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.intentAbstainAudit` required; add "intentAbstainAudit" to ALLOWED to get the CLI typo-guard. | server: within 60s (getServerFlag 60s in-process cache) — no deploy | Direct Admin SDK/Firestore-console write: delete/false the `config/flags.intentAbstainAudit` field. | functions/src/intents/extractIntents.js — persists abstained intent-extraction candidates for review; the OpenLoopsWidget never reads abstains regardless of this flag. [derived: read via getServerFlag() at functions/src/intents/extractIntents.js:330] | Persisted-abstain document count under the same `intents` collection extraction writes to. | Not in scripts/flip-flag.mjs ALLOWED — no CLI flip mechanism exists today. Must be set via a direct Admin SDK/Firestore-console write to `config/flags.intentAbstainAudit`; add the name to ALLOWED to wire up the typo-guarded CLI path. | No flip-flag.mjs entry (see mechanism column). |
| `intentExtraction` | OFF (false) | node scripts/flip-flag.mjs intentExtraction true | client: next app load; server: within 60s (getServerFlag 60s in-process cache) — no deploy | node scripts/flip-flag.mjs intentExtraction false | functions/src/intents/extractIntents.js — task/open-loop intent extraction, writes to the `intents` and `user_decisions` subcollections. [derived: read via getServerFlag() at functions/src/analysis/orchestrator.js:361] | `intents`/`user_decisions` document counts (owner-scoped Firestore query, no dashboard) for extraction volume; `intentAbstainAudit` (separate server-only flag, see below) gates whether abstained candidates are also persisted for review. | PRD 0B (I1-I4) — shipped, part of the R1 flag set. | — |
| `model.embeddingV2Read` | OFF (false) | node scripts/flip-flag.mjs model.embeddingV2Read true | client: next app load — no deploy | node scripts/flip-flag.mjs model.embeddingV2Read false | src/services/ai/embeddings.js — gates reading the v2 (gemini-embedding-2) index vs. the legacy embedding index during the dual-index migration; also declared server-side in functions/src/models/registry.js MODEL_FLAG_DEFAULTS, but the server accessor `getModelFlag()` has no call site (client-only in practice today). | Retrieval-quality spot checks (no dedicated log line found) — see embeddings.js's own flag-OFF-nuance comment for what silently changes when this flips. | Part of the 2026-07-22 embeddings-v2-migration plan (docs/superpowers/plans/2026-07-22-embeddings-v2-migration.md). | — |
| `model.embeddingWriteV2` | OFF (false) | node scripts/flip-flag.mjs model.embeddingWriteV2 true | server: within 60s (getServerFlag 60s in-process cache) — no deploy | node scripts/flip-flag.mjs model.embeddingWriteV2 false | functions/src/models/registry.js MODEL_FLAG_DEFAULTS — gates writing embeddings through the v2 (gemini-embedding-2) path during the dual-index migration. WIRED: read via `getModelFlag(db, 'model.embeddingWriteV2')` inside `generateEmbeddingInternal()` in the `functions/index.js` monolith (NOT `functions/src` — the exact file:line is auto-derived below). Flipping this ON activates the v2 dual-write for every subsequent embedding write; flipping it back OFF stops new v2 writes (existing v2 vectors already written are untouched). [derived: read via getModelFlag() at functions/index.js:491] | v2-embedding write volume — no dedicated log line found; watch `embeddingV2` field presence rate on newly-written entry docs (owner-scoped Firestore query) before/after flip, and the `model_manifest` log lines for the `embeddingV2` workload once a write-path call logs through `logModelManifest` (confirm at flip time — not verified as wired to that log call specifically). | Part of the 2026-07-22 embeddings-v2-migration plan (docs/superpowers/plans/2026-07-22-embeddings-v2-migration.md). Rollback = flip back to false via `node scripts/flip-flag.mjs model.embeddingWriteV2 false` — the v1 legacy path in `generateEmbeddingInternal()` runs independently in its own try/catch regardless of this flag, so flipping OFF does not remove v1 coverage. | — |
| `model.gemini35flash` | OFF (false) | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.gemini35flash` required; add "model.gemini35flash" to ALLOWED to get the CLI typo-guard. | unknown — see Gaps (no confirmed read site) | Direct Admin SDK/Firestore-console write: delete/false the `config/flags.model.gemini35flash` field. | DECLARED IN FLAG_DEFAULTS BUT UNUSED — no `getFlag('model.gemini35flash')` call site outside `src/config/__tests__/flags.test.js` (a generic getFlag-mechanism test, not a real feature gate). Not present in flip-flag.mjs ALLOWED or STRING_ALLOWED either. | N/A — nothing reads it. | Candidate for deletion per the plan's "delete expired flags" directive — confirm nothing external depends on the Firestore field, then remove from FLAG_DEFAULTS. | No flip-flag.mjs entry (see mechanism column). |
| `nativeBackgroundUpload` | OFF (false) | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.nativeBackgroundUpload` required; add "nativeBackgroundUpload" to ALLOWED to get the CLI typo-guard. | client: next app load — no deploy | Direct Admin SDK/Firestore-console write: delete/false the `config/flags.nativeBackgroundUpload` field. | CAP-01 (product review, P0): native direct-to-storage background upload for capture, landed code-complete but DARK. Client call sites now exist — src/components/dashboard/EntryBar.jsx (native stopRecording branch, enqueue), src/App.jsx (launch-time listener attach + reconcile) — both via src/services/capture/nativeBackgroundUpload.js, and the server (functions/src/capture/{uploadTicket,onAudioUploaded}.js) + Swift (ios/App/App/Capture/{CapturePlugin,BackgroundUploader}.swift) sides are complete. Every call site checks this flag first and no-ops when it is off (verified by dedicated flag-off-inertness unit tests) — flipping requires Michael's physical-device validation (docs/quality/device-validation-matrix.md rows 5, 6, 11-14) first. | Capture-upload retention sweep `count` field (functions/src/telemetry/stageLog.js META_WHITELIST), plus capture success/duplicate-recovery test coverage (device-validation-matrix.md rows 5, 6, 11-14). | CAP-01 ships dark this sprint — do not flip until Michael completes the physical-device matrix rows (11-14: suspend/force-kill mid-upload, airplane-mode retry, duplicate-finalize) and signs off. | No flip-flag.mjs entry (see mechanism column). |
| `openLoops` | OFF (false) | node scripts/flip-flag.mjs openLoops true | client: next app load — no deploy | node scripts/flip-flag.mjs openLoops false | OpenLoopsWidget + IntentSuggestionTray — in-app only, never notifies (R1). | `intents` subcollection document counts filtered to open-loop type (owner-scoped query); dismissal is final so a dismissal-rate spike after flip is the signal to watch. | R1, shipped. Michael's R2 veto-window checklist item (1) covers the batch this shipped alongside but does not itself gate `openLoops`. | — |
| `personalExperiments` | OFF (false) | node scripts/flip-flag.mjs personalExperiments true | client: next app load — no deploy | node scripts/flip-flag.mjs personalExperiments false | src/services/experiments/{templates,questionGate,estimator,computeResult,experimentsService,preflight}.js — entirely client-side 14/28-day observational experiments; plan-freeze + coverage-floor invariants enforced in firestore.rules and experimentsService.js. | `experiments` collection document counts and result-state distribution (insufficient vs. estimated) — owner-scoped Firestore query, no dashboard. | Checklist item (8): safety/data-method memo docs/quality/experiments-data-method.md SIGNED 2026-07-22 (both boxes). No composite index needed (item 10). | — |
| `reflectionRecipes` | OFF (false) | node scripts/flip-flag.mjs reflectionRecipes true | client: next app load — no deploy | node scripts/flip-flag.mjs reflectionRecipes false | src/services/reflections/ — versioned, scope+exclusion-honoring reflection recipes over the existing scope-filtered Ask Journal seams; reuses the client-side jsPDF export pathway. | No dedicated log line found — watch export success/failure via client error reporting (crashReporting.js). | Checklist item (5): 3rd in the recommended R2 flip order. | — |
| `serverAnalysisOrchestrator` | OFF (false) | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.serverAnalysisOrchestrator` required; add "serverAnalysisOrchestrator" to ALLOWED to get the CLI typo-guard. | client: next app load; server: within 60s (getServerFlag 60s in-process cache) — no deploy | Direct Admin SDK/Firestore-console write: delete/false the `config/flags.serverAnalysisOrchestrator` field. | functions/src/analysis/orchestrator.js — server-side entry analysis orchestration vs. the legacy client-driven path (functions/src/triggers/watchdogGuards.js, entryUpdateAnalysis.js both branch on this flag). [derived: read via getServerFlag() at functions/index.js:2183, functions/index.js:2312, functions/src/triggers/entryUpdateAnalysis.js:51, functions/src/triggers/watchdogGuards.js:36] | `model_manifest` structured log lines (functions/src/models/registry.js logModelManifest) for the `classify`/`analyze` workloads — volume and `ok`/`durationMs` after flip; watchdogGuards' stuck-run lease-timeout log for orphaned runs. | None on file beyond the general R1-era rollout posture. | No flip-flag.mjs entry (see mechanism column). |
| `sessionPrep` | OFF (false) | node scripts/flip-flag.mjs sessionPrep true | client: next app load — no deploy | node scripts/flip-flag.mjs sessionPrep false | src/services/reflections/ — since-date/scope-explicit brief + safety-reviewed jsPDF export (never emits `safety_flagged`/`has_warning_indicators` labels or a citation for an unsafe/removed source). | No dedicated log line found — export success/failure via crashReporting.js; safety-review correctness is covered by the validation matrix, not a runtime metric. | Checklist item (5): 4th in the recommended R2 flip order. | — |
| `voiceChapters` | OFF (false) | node scripts/flip-flag.mjs voiceChapters true | client: next app load — no deploy | node scripts/flip-flag.mjs voiceChapters false | Native `CaptureDraft` sidecar -> functions/src/transcription/fusedTranscription.js `computeChapterBoundaries` -> metadata-only `transcription.chapters` field. Raw text/rawTranscript/createdAt never touched by a chapter edit. | `model_manifest` log lines for the `fusedTranscription` workload (volume/`ok`); chapter-edit operations do not mutate raw fields by construction, so a raw-field diff on a chapter edit would indicate a regression. | Checklist item (3): Xcode simulator build DONE (2026-07-22); the on-device physical-mic `markChapter` check is still open — required before this reaches native users, per the recommended flip order (2nd). | — |
| `webChunkPersistence` | ON (true) | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.webChunkPersistence` required; add "webChunkPersistence" to ALLOWED to get the CLI typo-guard. | client: next app load — no deploy | Direct Admin SDK/Firestore-console write: delete/false the `config/flags.webChunkPersistence` field. | Additive web capture durability (chunked local persistence before upload). | Capture-stage telemetry `bytes`/`durationMs`/`queueDepth` fields (functions/src/telemetry/stageLog.js) for regressions after flip. | None — default-on, additive/safe per its own FLAG_DEFAULTS comment. | No flip-flag.mjs entry (see mechanism column). |

## Model workload overrides (`functions/src/models/registry.js` MODEL_DEFAULTS)

String-valued `model.<workload>` overrides on the same `config/flags` doc, resolved by `getModel()` (registry default if no override is set).

| Workload | Default model id | Prod flip mechanism | Rollback | Note |
|---|---|---|---|---|
| `analyze` | `gemini-3-flash-preview` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.analyze` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.analyze` field. |  |
| `chat` | `gpt-4o-mini` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.chat` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.chat` field. |  |
| `chatFallback` | `gpt-4o` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.chatFallback` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.chatFallback` field. |  |
| `classify` | `gemini-3-flash-preview` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.classify` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.classify` field. |  |
| `digest` | `gemini-3.5-flash` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.digest` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.digest` field. |  |
| `embedding` | `text-embedding-004` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.embedding` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.embedding` field. |  |
| `embeddingV2` | `gemini-embedding-2` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.embeddingV2` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.embeddingV2` field. |  |
| `entityResolution` | `gemini-3-flash-preview` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.entityResolution` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.entityResolution` field. |  |
| `fusedTranscription` | `gemini-2.5-flash` | node scripts/flip-flag.mjs model.fusedTranscription <gemini-3.5-flash \| gemini-2.5-flash \| default> | node scripts/flip-flag.mjs model.fusedTranscription default | Wired via flip-flag.mjs STRING_ALLOWED (accepts gemini-3.5-flash/gemini-2.5-flash/default). |
| `insight` | `gemini-3-flash-preview` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.insight` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.insight` field. |  |
| `insightVerifier` | `gemini-3-flash-preview` | node scripts/flip-flag.mjs model.insightVerifier <gemini-3.5-flash \| gemini-3-flash-preview \| default> | node scripts/flip-flag.mjs model.insightVerifier default | Wired via flip-flag.mjs STRING_ALLOWED (R4 Phase 2 claim-wording verifier). See insightWriter note. |
| `insightWriter` | `gemini-3.5-flash` | node scripts/flip-flag.mjs model.insightWriter <gemini-3.5-flash \| gemini-3-flash-preview \| default> | node scripts/flip-flag.mjs model.insightWriter default | Wired via flip-flag.mjs STRING_ALLOWED (R4 Phase 2 claim-wording writer). Deliberately defaults to a DIFFERENT model than insightVerifier — do not set both the same without reading registry.js's comment first. |
| `intentExtraction` | `gemini-3.5-flash` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.intentExtraction` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.intentExtraction` field. |  |
| `realtimeNA` | `gpt-realtime-2.1` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.realtimeNA` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.realtimeNA` field. | Owned by relay-server via env var REALTIME_MODEL, not this registry — recorded here for inventory completeness only (per registry.js's own comment); no flip-flag.mjs mechanism applies. |
| `temporal` | `gemini-3.5-flash` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.temporal` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.temporal` field. |  |
| `tone` | `gemini-3.5-flash` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.tone` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.tone` field. |  |
| `transcriptionFallback` | `whisper-1` | NOT WIRED in scripts/flip-flag.mjs — direct Admin SDK/Firestore-console write to `config/flags.model.transcriptionFallback` required. | Direct Admin SDK/Firestore-console write: delete the `config/flags.model.transcriptionFallback` field. |  |

## Dark code constants (no Firestore doc at all)

`export const SCREAMING_CASE = true|false;` kill switches. Rollback requires a code change + redeploy, never a flip-flag.mjs invocation.

| Constant | Current value | Declared in | Rollback | Surface | KPI to watch after flip | Owner-gate notes |
|---|---|---|---|---|---|---|
| `LLM_WRITER_ENABLED` | OFF (false) | `src/services/insights/claims/claimsPipeline.js` | Edit the constant in src/services/insights/claims/claimsPipeline.js + redeploy (git commit -> push to main -> CI deploy). No Firestore involved. | functions/src/insights/{claimWriter,claimVerifier}.js + the `writeClaimWording` callable — the ONLY path an LLM can ever author a claim's `wording` (never its evidence). Rides the `insightClaims` flag as a prerequisite but is an independent code-level gate on top of it. | `model_manifest` log lines (functions/src/models/registry.js logModelManifest) for the `insightWriter`/`insightVerifier` workloads once flipped; claimsPipeline.js emits one `console.warn` per RUN (not per claim) when the LLM writer path falls back to the deterministic template — a sustained non-zero rate of that warning after flip is the signal to watch. | Checklist item (12): no memo required — decide after eyeballing verifier behavior on real data (dev-build flip or ask for flag promotion). |
| `USE_FUSED_TRANSCRIPTION` | ON (true) | `src/config/ai.js` | Edit the constant in src/config/ai.js + redeploy (git commit -> push to main -> CI deploy). No Firestore involved. | src/config/ai.js — kill switch; `false` restores the legacy whisper-1 + separate tone pipeline (transcribeWithTone) with no server redeploy required on the FUNCTIONS side (the constant itself still needs a frontend rebuild+deploy to flip, since it is baked into the client bundle at build time). | `model_manifest` log lines for the `fusedTranscription` / `transcriptionFallback` workloads — a shift in which workload is being resolved after flip is directly visible there. | None on file. |

## Retired dark constants (historical)

- **`RISKY_CLAIMS_ENABLED`** — Retired R4 Phase 3 (2026-07-23, plan docs/superpowers/plans/2026-07-23-r4-phase3-action-loop.md, decision P3-D1). Gated four claim types (counterfactuals, belief dissonance, intervention outcomes, personalized recommendations); the modules using it (`counterfactual.js`, `beliefDissonance.js`, `interventionTracker.js`) were deleted whole and `recommendationEngine.js` reduced to a generic ideas-only engine — nothing left to gate. Only historical tombstone comments and a test-description string remain (src/services/nexus/orchestrator.js, src/__tests__/validationMatrix.test.js).

