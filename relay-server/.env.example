# OpenAI API Key (required)
OPENAI_API_KEY=sk-...

# Firebase Service Account JSON (for local development)
# In Cloud Run, use default application credentials instead
FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Server port (default: 8080)
PORT=8080

# Environment (development/production)
NODE_ENV=development

# Whoop Integration (optional - for cloud health data sync)
# Register at https://developer.whoop.com
WHOOP_CLIENT_ID=your-whoop-client-id
WHOOP_CLIENT_SECRET=your-whoop-client-secret
WHOOP_REDIRECT_URI=https://echo-vault-app.web.app/__/auth/handler

# Whoop Token Encryption Key (32-byte base64 encoded)
# Generate with: openssl rand -base64 32
WHOOP_TOKEN_ENCRYPTION_KEY=your-base64-encoded-32-byte-key
